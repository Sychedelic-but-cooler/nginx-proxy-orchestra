# Nginx Proxy Orchestra Configuration
# HTTPS server port (admin interface)
PORT=81

# Database path (relative to project root)
DB_PATH=./data/database.sqlite

# Nginx configuration directory (absolute path)
# This is where proxy configs will be written
# Default: ./data/conf (allows running without sudo)
NGINX_CONFIG_DIR=/nginx-proxy-orchestra/data/conf

# SSL certificate storage directory (absolute path)
# Default: ./data/ssl
SSL_DIR=/nginx-proxy-orchestra/data/ssl

# Nginx access log path for statistics
# Default: /var/log/nginx/access.log
NGINX_ACCESS_LOG=/var/log/nginx/access.log

# Environment (development or production)
NODE_ENV=production
# JWT Secret for token signing (auto-generated if not provided)
JWT_SECRET=CHANGE_ME
